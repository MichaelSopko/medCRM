query therapists($clinic_id: Int!) {
    therapists(clinic_id: $clinic_id) {
        id
        first_name
        last_name
        email
        license_number
        phone
        birth_date
        id_number
    }
}