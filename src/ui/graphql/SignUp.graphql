mutation signUp(
    $first_name: String
    $last_name: String
    $email: String!
    $password: String!
) {
    signUp(first_name: $first_name, last_name: $last_name, email: $email, password: $password) {
        status
    }
}