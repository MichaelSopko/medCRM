query administrators {
    administrators {
        id
        first_name
        last_name
        email
        clinic {
            id
            name
        }
    }
    clinics {
        id
        name
    }
}