#import './ClinicFragment.graphql'

mutation editClinic(
    $id: Int!
    $clinic: ClinicInput!
) {
    editClinic(
        id: $id,
        clinic: $clinic
    ) {
        ...ClinicFragment
    }
}
