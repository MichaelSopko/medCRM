#import './PatientFragment.graphql'

mutation editPatient(
$id: Int!
    $patient: PatientInput!
) {
    editPatient(
        id: $id
        patient: $patient
    ) {
        ...PatientFragment
    }
}